<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.6</version>
        <relativePath/>
    </parent>

    <groupId>com.aurora</groupId>
    <artifactId>aurora-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>aurora-parent</name>
    <description>AURORA 综合一体化后台平台 - 父模块</description>

    <modules>
        <module>aurora-common</module>
        <module>aurora-system</module>
        <module>aurora-hr</module>
        <module>aurora-edu</module>
        <module>aurora-oj</module>
        <module>aurora-mall</module>
        <module>aurora-ai</module>
        <module>aurora-server</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 第三方依赖版本（Task 0.A.2 会补齐此处） -->
        <mybatis-plus.version>3.5.10</mybatis-plus.version>
        <hutool.version>5.8.43</hutool.version>
        <knife4j.version>4.5.0</knife4j.version>
        <lombok.version>1.18.36</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 内部模块互相引用 -->
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-system</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-hr</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-edu</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-oj</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-mall</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aurora</groupId>
                <artifactId>aurora-ai</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- 第三方锁版本 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
